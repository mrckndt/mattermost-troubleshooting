GITHUB_PERSONAL_ACCESS_TOKEN=
GITHUB_READ_ONLY=true

# Optional: restrict toolsets (comma-separated).
# Default: context,repos,issues,pull_requests,users
# GITHUB_TOOLSETS=context,repos,issues,pull_requests
