# Optional MCP server for the investigation pipeline: internal Jira.
# sooperset/mcp-atlassian, run as a long-lived SSE service.
# Credentials and tunable settings (READ_ONLY_MODE, JIRA_PROJECTS_FILTER) live in mcp/atlassian/.env (gitignored).
# Start:   docker compose -f mcp/atlassian/docker-compose.yml up -d
# Update:  docker compose -f mcp/atlassian/docker-compose.yml pull && docker compose -f mcp/atlassian/docker-compose.yml up -d
services:
  mcp-atlassian:
    image: ghcr.io/sooperset/mcp-atlassian:latest
    container_name: mcp-atlassian
    env_file: .env
    ports:
      - "7080:7080"
    environment:
      - TRANSPORT=sse
      - PORT=7080
      - HOST=0.0.0.0
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:7080/healthz')"]
      interval: 30s
      timeout: 10s
      retries: 3
